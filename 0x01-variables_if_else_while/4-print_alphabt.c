#include <stdio.h>

/**
 * main - prints the aphabet in lowercase followed by a newline,exclude qande
 * Return: 0
 */
int main(void)
{
	char la;

	for (la = 'a'; 'la' <= 'z' la++)
	{
		if (la != 'e' && la != 'q')
			putchar(la);
	}

	putchar('\n');

	Return (0);
}
